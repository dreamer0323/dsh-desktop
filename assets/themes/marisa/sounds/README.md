# 事件音效 / 语音

把你**拥有使用权**的音频放到这个目录，按事件命名。支持 `.mp3 / .wav / .ogg / .m4a`。

```
click.mp3      # 按钮点击
hover.wav      # 悬停控件
complete.mp3   # 生成完成
error.ogg      # 出错
```

- 未放置文件时，自动用 WebAudio 合成占位音（无需任何二进制资源）。
- 文件名大小写敏感，需与事件名完全一致。
